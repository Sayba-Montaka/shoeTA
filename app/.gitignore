i want that,every new registered factory gets 10 free token. when factory post a job he has to pay 10 taka for each token which duration is 1 week...but first 10 token is free where 1 token contains 1 week ..if a user clicks on 4 week then 4 token waste and left 6 token... after 10 token he has to pay for each token..in PostJob :<EditText     android:id="@+id/etContactPhone"     android:layout_width="match_parent"     android:layout_height="54dp"     android:hint="01XXXXXXXXX"     android:inputType="phone"     android:background="@drawable/bg_input"     android:paddingStart="18dp"     android:paddingEnd="18dp"     android:textColor="@color/text_primary"     android:textColorHint="@color/text_hint"     android:textSize="15sp"     android:layout_marginBottom="20dp"/> after that there will be a section which have spinner :apni koto week er jnno post korte chan? if user clicks on 1 week , then he has to pay 10 taka,,,if he clicks on 2 week he has to pay 20 taka,,if he clicks 3 week ,then he has to pay 30 taka ,if he clicks 4 week he has to 40 taka,,the spinner contains 4 weeks only..should i use SSL E-commerce? can you make this?